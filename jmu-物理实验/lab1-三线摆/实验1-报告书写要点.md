# 实验报告书写要点（评分标准）

## 一、实验数据与处理部分

1. 使用三线表格罗列实验数据；
2. 采用肖维涅法则对数据进行检验，将坏值剔除；
3. 逐个算出各直接测量量的平均值、不确定度和相对不确定度的处理结果；
   （注：平均值和标准偏差直接给出计算结果；其他计算过程要满足有公式、数据代入和计算结果及单位三个要素。）
4. 间接测量量的计算（代入数据并计算结果）；
5. 间接测量量不确定度的计算（代入数据并计算结果）。
   （注：中间计算过程，不确定度保留 2 个有效数字）

## 二、实验结果部分

实验结果应先用文字叙述清楚，再给出规范的结果表达式。
（注：不确定度的表达结果保留 1 个有效数字，遵循"逢数进位"原则；平均值结果的尾数应与不确定度的尾数对齐，遵循"四舍六入五凑偶"原则。）

---

# 三线摆法测定物体转动惯量

## 数据处理示例：

### 三线摆法测定物体转动惯量实验数据记录表

| $m(g)$ | $M(g)$ | $a(cm)$ | $b(cm)$ | $H_0(cm)$ | $t_0(s)$ | $H_1(cm)$ | $t_1(s)$ |
|:-:|:-:|:-:|:-:|:-:|:-:|:-:|:-:|
|   |   |   |   |   |   |   |   |


---

### 1. 采用肖维涅法则对表中各项实验数据进行剔除坏值检查，给出检查结果。

（检查过程无需写出！）

### 2. 各直接测量物理量不确定度的计算

电子天平：$\Delta_m = 0.05g$，游标卡尺：$\Delta_m = 0.02mm$，秒表：$\Delta_m = 0.05s$，米尺：$\Delta_m = 0.05cm$

$$
u_M = \frac{\Delta_m}{\sqrt{3}} =
$$

$$
\bar{a} = \qquad,\qquad S_a = \qquad,\qquad
u_A = \frac{S_a}{\sqrt{n}} = \qquad,\qquad
u_B = \frac{\Delta_m}{\sqrt{3}} =
$$

$$
u_a = \sqrt{u_A^2 + u_B^2} =
$$

分别计算得到 $b,H_0,t_0,H_1,t_1$ 的不确定度的大小。

### 3. 计算空悬盘的转动惯量及其不确定度（数据要代入，结果要加单位）

$$
I_0 = \frac{mgab}{12\pi^2 H_0} T_0^2 = \frac{mgab}{12\pi^2 H_0} \cdot \left(\frac{t_0}{50}\right)^2 =
$$

$$
u_{I_0} = I_0 \sqrt{\left(\frac{u_a}{a}\right)^2 + \left(\frac{u_b}{b}\right)^2 + \left(\frac{u_{H_0}}{H_0}\right)^2 + \left(\frac{2u_{t_0}}{t_0}\right)^2} =
$$

### 4. 计算载物悬盘的转动惯量及其不确定度（数据要代入，结果要加单位）

$$
I_1 = \frac{(M+m)gab}{12\pi^2 H_1} T_1^2 = \frac{(M+m)gab}{12\pi^2 H_1} \cdot \left(\frac{t_1}{50}\right)^2 =
$$

$$
u_{I_1} = I_1 \sqrt{\left(\frac{u_a}{a}\right)^2 + \left(\frac{u_b}{b}\right)^2 + \left(\frac{u_{H_1}}{H_1}\right)^2 + \left(\frac{2u_{t_1}}{t_1}\right)^2 + \left(\frac{u_M}{M+m}\right)^2} =
$$

### 5. 计算不规则测件的转动惯量（数据要代入，结果要加单位）

$$
I = I_1 - I_0 =
$$

$$
u_I = \sqrt{u_{I_0}^2 + u_{I_1}^2} =
$$

### 6. 实验结果的表达

三线摆法测定不规则物体的转动惯量的测量结果为：

$$
I = \bar{I} \pm u_I = \qquad,\qquad (P = 68.3\\%)
$$

> （注：结果用科学计数法表示，要有相应的单位，不确定度保留 1 个有效数字。）

## 实验 3：Linux 文件系统管理

### 4、实验过程
#### (1)目录操作  
在当前目录下创建一个子目录，对其进行相应，然后再删除它（要求每步都要使用 ls
命令观察结果）：  
- a. 创建目录：mkdir dir1 dir2  
- b. 复制文件或目录：cp -r /etc/rc.d dir1  
- c. 复制文件或目录：cp -r /etc/rc.d dir3  

![1](1.png)

好像没找到/etc/rc.d文件, 搜一搜资料看一下：

> gpt的回答：  
>> 许多现代 Linux 发行版使用 systemd 作为初始化系统（init system），从而取代了传统的 /etc/rc.d 和基于运行级别的服务管理方式。这意味着在 systemd 系统上，服务和启动脚本都由 systemctl 和对应的 /etc/systemd/ 目录进行管理。

那就换一个路径：
![2](2.png)
66, 好像用-r参数 可以把文件夹里面的子文件全部复制

那如果试试看不带参数复制文件夹呢？
![3](3.png)
好像确实不可以

d. 删除目录（方法 1）  ：rmdir dir1 dir2  
e. 删除目录（方法 2）  ：rm –rf dir1  

![4](4.png)



f. 观察 dir3 的内容后，删除之。  

![5](5.png)



#### (2)文件的硬链接与符号链接 

创建一个文件myfile 和一个目录mydir，
![6](6.png)
分别为myfile 创建一个硬链接和一个符号链接：  
```bash   
ln  myfile  /tmp/myfile  
ln  -s  myfile  /tmp/myfile1    
ln  -s  mydir  /tmp/myd   
```
使用ls 命令查看结果。   

![7](7.png)

<u><span style="color:lightblue;">对了，ln指令是干什么的？ 用tldr看看：</span></u>


> ```shell
> vincentzyu@vincentzyu:~/Documents/jmu-course/jmu-basic/labs/lab3/> labdir$ tldr ln
> ln
> Create links to files and directories.More information: https://www.gnu.org/software/coreutils/ln.
> 
>  - Create a symbolic link to a file or directory:
>    ln -s {{/path/to/file_or_directory}} {{path/to/symlink}}
> 
>  - Overwrite an existing symbolic link to point to a different file:
>    ln -sf {{/path/to/new_file}} {{path/to/symlink}}
> 
>  - Create a hard link to a file:
>    ln {{/path/to/file}} {{path/to/hardlink}}
> 
> ```

再问问gpt

```md
1. ln file1 file2

    功能：创建一个硬链接。
    说明：
        file1 是源文件（被链接的文件）。
        file2 是硬链接的名称。
        硬链接是指向文件内容的直接指针，file2 将与 file1 指向同一个文件数据。
        如果 file1 被删除，file2 仍然可以访问文件的内容。
        硬链接不能跨文件系统（不同的分区），且不能链接目录（除非使用超级用户权限）。

2. ln -s file1 file2

    功能：创建一个符号链接（软链接）。
    说明：
        -s 选项表示创建符号链接。
        file1 是源文件（被链接的文件）。
        file2 是符号链接的名称。
        符号链接是指向文件路径的快捷方式，它并不直接指向文件内容，而是包含了对 file1 的路径。
        如果 file1 被删除，file2 将变成一个无效链接（断开的链接），因为它指向的路径不再存在。
        符号链接可以跨文件系统，并且可以链接目录。

3. ln -s dir1 dir2

    功能：创建一个指向目录的符号链接。
    说明：
        -s 选项表示创建符号链接。
        dir1 是源目录（被链接的目录）。
        dir2 是符号链接的名称。
        dir2 将成为 dir1 的快捷方式，通过访问 dir2 可以访问 dir1 目录的内容。
        同样，如果 dir1 被删除，dir2 将变成无效链接。

总结

    ln file1 file2：创建一个硬链接 file2 指向文件 file1，两者共享同一文件内容。
    ln -s file1 file2：创建一个符号链接 file2 指向 file1 的路径，file2 是 file1 的快捷方式。
    ln -s dir1 dir2：创建一个符号链接 dir2 指向目录 dir1，dir2 是 dir1 的快捷方式。
```
 
soga, 这么神奇的？ 操作一下试试：

```bash
sudo vim /tmp/myfile
```

![8](8.png)

![9](9.png)  

<u><span style="color:lightblue;">诶？用vim的话  好像不能用这个硬链接来写文件？</span></u>


用echo+重定向试试看：
![10](10.png)
可以！
可见，我刚刚使用了两次echo+重定向，来往文件里输入内容，
输入的内容都是qwq
而第一次的重定向路径参数是指针名`/tmp/myfile`
第二次用的是被链接文件的路径`./myfile`

然后两次都使用`cat /tmp/myfile` 来查看文件内容，发现两次的echo都成功往文件里输入内容

接着再把源文件.myfile删了，然后试试看/tmp/myfile能不能访问到这个文件呢？  
![11](11.png)
还真可以！  

to be honest, `ln file1 file2` 然后file1删了，file2仍然能够访问到这个文件，第一次听上去 会比较反直觉， 于是我就去问gpt：

我的提问：
```
ln file1 file2

第一次看到这个用法的时候会有点反直觉，因此引人思考：

文件实际上存储在别处，在物理内存中不一定按照文件树 的逻辑存储， 但是文件树把不同地方的内存组织起来？

一旦使用ln file1 file2， 那么file1 file2将指向同一个内存对应的文件？

我说的对吗 有需要纠正的吗
```  


gpt的回答：
```
你说得很对，关于硬链接和文件存储的理解非常深入。让我稍微整理和补充一下你的理解：
硬链接和文件存储

    文件存储和物理地址：
        文件在磁盘上以数据块的形式存储，而不是简单地按照文件名的逻辑顺序存储。文件系统使用 inode（索引节点）来管理文件的元数据（如文件大小、创建时间、权限等）和数据块的物理地址。
        每个文件在文件系统中都有一个唯一的 inode。inode 存储了指向文件数据块的指针。

    硬链接的机制：
        当你执行 ln file1 file2 时，实际上是创建了一个新的目录项（file2），这个目录项指向同一个 inode，即相同的文件数据。换句话说，file1 和 file2 都指向同一个物理文件内容。
        这就是为什么删除 file1 后，file2 仍然可以访问文件内容的原因。只有在所有硬链接都被删除后，文件数据才会被真正从磁盘中清除。

    文件树与逻辑结构：
        文件树（目录结构）是一种逻辑组织方式，用于帮助用户和操作系统管理文件。它通过目录和文件名的关系建立起文件的层次结构。
        硬链接使得同一个文件可以通过不同的路径（不同的文件名）访问，这种方式在逻辑上可能会引起混淆，因为看起来有多个文件，但实际上它们都指向同一个物理内容。

总结

你的理解是准确的，ln file1 file2 使得 file1 和 file2 都指向同一个 inode，这两个文件名只是逻辑上的别名，背后却是同样的数据存储在磁盘上。这种特性在文件管理和节省存储空间方面非常有用。
```

<br>

接下来，再去看看 符号链接/tmp/myfile1
刚刚把myfile删了， 先创建回来，然后检查一下链接：
![12](12.png)
 
然后分别对源文件和符号链接进行echo重定向：
![13](13.png)

耶？ 好像对/tmp/myfile1这个符号链接的echo重定向不管用？？

检查一下权限 再试试？  
![14](14.png)
好像还是不行？

那试试看： 先把现在的符号链接删了，然后重新创建，但这次改用绝对路径：  
![15](15.png)

ok，这次可以了， 可见，对于源文件`./myfile`和符号链接`/tmp/myfile1`, echo指令都是可以进行重定向的

<br>

接着再试试 对于文件夹的符号链接：
![16](16.png)

还是类似的，刚开始我用/tmp/myd这个符号链接作为路径的一部分，然后好像找不到这个文件


<u><span style="color:lightblue;">后面我把符号链接删了 改成绝对路径就好使了，见上图↑</span></u>




#### (3)文件查找  
说明：如果查找的文件不存在，可以事先创建之，以供实验之用。   
##### a. 在目录/tmp 和/home 下查找24 小时内没有更改的文件。   
```bash
find /tmp /home –mtime –1 –print  
```

<br>
... 这个指令好像输出的东西有亿点多:   

![17](17.gif)
<br>
换一个路径试试：  

```bash
vincentzyu@vincentzyu:~/Documents/jmu-course$ find ./ -mtime -1 -print
```

![18](18.png)

ovo， PDF里面的文字好像写反了，这个指令后面三个参数的意思好像是 输出24小时内被更改过的文件， 问题不大

##### b. 在/home 下查找属于用户gjshao 的c 语言程序文件。  
```bash
find  /home  –user  gjshao  –name  ”*.c”  –print  
```

看来find这个工具能指定的参数挺多的，修改一下参数：
在/home/vincentzyu/Documents/jmu-course 下查找属于用户vincentzyu 的markdown文件。  

```bash
find /home/vincentzyu/Documents/jmu-course -user vincentzyu -name "*.md" -print

find /home/vincentzyu/Documents/playfield-learning -user vincentzyu -name "*.md" -print
```

![19](19.png)


##### c. 将当前目录内的所有*.c 和*.h 的文件打成tar 包/tmp/mych.tar 
tar cvf /tmp/mych.tar `find . –name ”*.[ch]” –type f –print` 



##### d. 在/tmp 目录内查找文件*.tmp，然后删除之 
find  /tmp –name ”*.tmp”  -exec rm {} \; 
